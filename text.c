#include "caso.h"

int
ulcd_move_cursor(struct ulcd_t *ulcd, unsigned int line, unsigned int column)
{
    return 0;
}

/*
    def txt_MoveCursor(self, line, column):
        return self.send_args_ack(self.MOVE_CURSOR, line, column)

*/
